/** Objetos de estilo puros para mocks de teste (sem styled-components). */
export {
  formTabStyles,
  confirmationModalStyles,
  confirmationModalWidth,
  editableTableStyles,
} from "../design-system/estilos";
